import { Inter, Manrope } from "next/font/google";

export const inter = Inter({
  variable: "--font-body",
  subsets: ["latin"],
});

export const manrope = Manrope({
  variable: "--font-headline",
  subsets: ["latin"],
});

export const interLabel = Inter({
  variable: "--font-label",
  subsets: ["latin"],
});
